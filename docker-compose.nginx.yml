version: '3'

networks:
  tickdocker_influxnet:
    external: true

services:
  nginx:
    image: nginx:1.13.8-alpine
    volumes:
      - ./etc/nginx/:/etc/nginx/
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    networks:
      - tickdocker_influxnet
